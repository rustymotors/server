{
  "name": "@mco-server/types",
  "version": "1.0.0",
  "main": "src/index.ts",
  "license": "MIT",
  "dependencies": {
    "@types/sqlite3": "3.1.7",
    "sqlite": "4.0.23"
  },
  "devDependencies": {
    "@babel/cli": "7",
    "@babel/core": "7",
    "@types/babel__core": "^7",
    "@types/eslint": "^7",
    "@types/prettier": "^2",
    "@typescript-eslint/eslint-plugin": "4",
    "@typescript-eslint/parser": "4",
    "eslint": "7",
    "eslint-config-prettier": "8",
    "jest": "27.2.1",
    "prettier": "2",
    "prettier-eslint": "13",
    "ts-jest": "27.0.5",
    "typescript": "4.4.3"
  },
  "scripts": {
    "preinstall": "npx only-allow yarn",
    "audit": "yarn npm audit",
    "check:license": "yarn licenses audit --summary --config=licenses.config.js",
    "start": "ts-node bin/app.ts",
    "check:type": "tsc --noEmit",
    "check:type:watch": "tsc --noEmit --watch",
    "build": "tsc --build",
    "build:clean": "tsc --build --clean",
    "lint": "eslint ./**/*.ts && prettier --write src test",
    "test": "yarn run lint-test",
    "lint-test": "yarn run lint && yarn run test:only",
    "test:only": "jest",
    "precommit": "yarn run lint-test"
  }
}
