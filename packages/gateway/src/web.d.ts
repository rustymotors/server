/**
 * Add web routes to the web server
 *
 * @param {import("fastify").FastifyInstance} webServer The web server
 */
export declare function addWebRoutes(
    webServer: import("fastify").FastifyInstance,
): Promise<void>;
